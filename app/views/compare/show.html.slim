h1
  = link_to t('broker.menu.search'), search_path
  | &nbsp; &laquo; &nbsp;
  = t('compare.show.header')
hr

table#compare.table.table-bordered.table-hover
  - @properties.each do |property|
    == render :partial => 'properties', locals: {:property => property.first, :services => @services, :services_count => @services_count}